<% if (typeof message !== 'undefined') { %>
    <div class=message
    onclick="destroyEvent(event)" >
        <%= message %>
    </div>
    <% } %>
    <% if (typeof error !== 'undefined') { %>
        <div class=error
        onclick="destroyEvent(event)" >
            <%= error %>
        </div>
        <% } %>